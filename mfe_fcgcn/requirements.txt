# MFE-FCGCN — Python dependencies
# Python 3.11 (paper implementation); 3.10+ should work.
#
# Install:
#   pip install -r requirements.txt
#
# For GPU support, install PyTorch with the appropriate CUDA version first:
#   https://pytorch.org/get-started/locally/
# Then install torch_geometric matching your torch+CUDA combination:
#   https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html

torch>=2.1.0                  # deep learning framework — §3.3
torch_geometric>=2.4.0        # graph neural networks (GCNConv, DataLoader, etc.) — §3.3
mne>=1.5.0                    # EEG file reading and PSD computation — §3.1
numpy>=1.24.0                 # numerical arrays
pandas>=2.0.0                 # CSV I/O and dataframe operations
scikit-learn>=1.3.0           # mutual_info_regression, StratifiedKFold, metrics — §3.2, §4.1
pyyaml>=6.0                   # config loading
tqdm>=4.65.0                  # progress bars
